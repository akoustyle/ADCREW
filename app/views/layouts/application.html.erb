<!DOCTYPE html>
<html>
  <head>
    <title>AD CREW</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="nav navbar navbar-inverse">
      <div class="container">
        <div class="navbar-brand" style="display: flex; justify-content: space-between;">
          <%= link_to "Insta", root_path %>
          <% if user_signed_in? %>
            <ul class="nav navbar navbar-left" style="display: flex; justify-content: end;">
              <li><%= link_to "Gram It!", new_campaign_path %></li>
              <li><%= link_to "Settings", edit_user_registration_path %></li>
              <li><%= link_to "Sign out", destroy_user_session_path, method: :delete %></li>
            </ul>
          <% else %>
            <ul class="nav navbar navbar-left">
              <li> <%= link_to "Log In", new_user_session_path %> </li>
          <% end %>
            </ul>
        </div>
      </div>
    </div>
    <div class="container">
    <%# <%= flash.each do |name, msg| %>
      <%# <%= content_tag :div, msg, class: "alert alert-in" %>
    <%# <% end %>
    <%= yield %>
    </div>
  </body>
</html>

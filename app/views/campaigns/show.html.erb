<h1><%= @campaign.title %> </h1>
<p><%= @campaign.description %></p>

<p> Post by <%= @campaign.user.email %></p>

<%= link_to "Back", root_path %>
<%= link_to "Edit", edit_campaign_path %>
<%= link_to "Delete", campaign_path, method: :delete, data: { confirm: "Are you sure?"} %>
